# Eğitim Evi site ayarları: ÖRNEK. Sunucuda data/config.yml olarak kopyala
# ve doldur. data/config.yml depoya girmez (.gitignore).

# Sayfaların altındaki "İletişim" satırı. Boş bırakılan görünmez.
iletisim:
  eposta: ""
  telefon: ""

# Sitenin üst şeridindeki "Android uygulaması" düğmesi. Boşsa GitHub'daki son sürüm açılır;
# Play Store'a çıkınca oranın adresi yazılır (https ile başlamalı).
uygulama:
  android: ""
